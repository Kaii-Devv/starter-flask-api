import datetime

# Mendapatkan waktu saat ini
now = datetime.datetime.now()

# Menambahkan 10 hari ke waktu saat ini
future_time = now + datetime.timedelta(days=10)

# Mengonversi waktu menjadi timestamp dalam detik
timestamp = int(future_time.timestamp()*1000)

print(timestamp)
